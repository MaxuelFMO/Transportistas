# React + Vite

El sistema se debe de inicializar con los 2 comandos siguietes.

```bash
npm install
```

Para el frontend

```bash
npm run dev
```

Para el backend

```bash
npm run server
```

La base de datos se encuentra en la carpeta src/db/transport.db.
Esta se inicializa automaticamente al ejecutar el comando npm run server
